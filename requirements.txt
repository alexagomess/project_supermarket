# Dependências diretas dos scripts de ETL (raw/cleaned/trusted).
# As dependências transitivas são resolvidas automaticamente pelo pip.

# Processamento de dados
pandas==2.2.3
numpy==2.1.2
openpyxl==3.1.5
Unidecode==1.3.8

# Web scraping (camada raw)
requests==2.32.3
beautifulsoup4==4.12.3

# Banco de dados
SQLAlchemy==1.4.54
psycopg2==2.9.10

# Google Drive
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1

# Configuração
python-dotenv==1.0.1

# Lakehouse (camadas cleaned/trusted em Delta Lake com Polars)
polars==1.9.0
deltalake==0.21.0

# Orquestração (DAGs) — o runtime roda via imagem Docker (apache/airflow:3.0.6);
# necessário localmente apenas para desenvolver/validar os DAGs.
apache-airflow==2.10.3
